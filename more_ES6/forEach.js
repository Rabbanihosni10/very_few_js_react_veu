const numbers=[1,2,4,5,6,10,45,3,7];

// forEach ==> exechte specific task without console the task result
// map ==> do the same task with console the task

numbers.forEach(num=>{
    const doubled = num*2;
    console.log(doubled);
})